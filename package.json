{
  "name": "cti-video-generator",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute cti-video-generator --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute cti-video-generator --file=./schema.sql --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
